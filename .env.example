# 기존 (Phase 4 이후 제거 가능)
NEXT_PUBLIC_APP_ID=
NEXT_PUBLIC_APP_KEY=
NEXT_PUBLIC_API_URL=

# 데이터베이스 (Docker)
DATABASE_URL=postgresql://chatbot:chatbot123@localhost:5432/chatbot

# JWT (RS256)
# 키 생성: npx tsx scripts/generate-keys.ts
JWT_PUBLIC_KEY=
JWT_PRIVATE_KEY=
JWT_ISSUER=dgist-auth
JWT_AUDIENCE=dgist-chatbot

# API Key 암호화 (AES-256-GCM)
# 생성: openssl rand -base64 32
ENCRYPTION_KEY=

# 인증 모드 ('mock' 또는 'legacy')
AUTH_MODE=mock
LEGACY_AUTH_API_URL=https://portal.dgist.ac.kr/api/auth/login
